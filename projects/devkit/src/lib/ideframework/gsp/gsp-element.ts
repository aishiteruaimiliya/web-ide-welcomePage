export class GSPElement {





  // methods to adapt gsp workspace. atom workspace element extends this from HTMLElement.
  // should be invoked by workspace component so resolve ui affects.
  addGSPListener(event: string, handler: () => any, flag?: boolean) {

  }

}
